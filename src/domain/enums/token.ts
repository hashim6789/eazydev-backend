/**
 * Enum representing error types related to token operations.
 *
 * @enum
 */
export enum TokenErrorType {
  TokenFetchingFailed = "Token fetching is failed!",
  TokenCreationFailed = "Token creation is failed!",
  TokenNotFound = "Token is not found!",
  TokenIsNotValid = "Token is not valid!",
}
