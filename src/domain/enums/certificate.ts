/**
 * Enum representing error types related to certificate operations.
 *
 * @enum
 */
export enum CertificateErrorType {
  CertificateFetchingFailed = "Certificate fetching is failed!",
  CertificateCreationFailed = "Certificate creation is failed!",
  CertificateNotFound = "Certificate is not found!",
}
