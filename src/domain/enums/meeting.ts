/**
 * Enum representing error types related to meeting operations.
 *
 * @enum
 */
export enum MeetingErrorType {
  MeetingFetchingFailed = "Meeting fetching is failed!",
  MeetingCreationFailed = "Meeting creation is failed!",
  MeetingNotFound = "Meeting is not found!",
}
